{
  "name": "financial-ai-manager",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace server dev\" \"yarn workspace client dev\"",
    "build": "yarn workspace server build && yarn workspace client build",
    "db:seed": "yarn workspace server db:seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
